{
  "name": "gcal-sync",
  "version": "0.1.0",
  "description": "Calendar busy-time sync across multiple providers",
  "scripts": {
    "bundle": "node scripts/bundle.js",
    "push": "clasp push --rootDir ./apps-script",
    "preview": "node build-preview.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "jest": "^29.0.0"
  }
}
